---
kind: prediction
about: 16-work-order-page-reader.md
written: 2026-09-20T15:24+10:00
by: dispatcher (main session)
---

# What I expect the page-reader to find

Written before the role was dispatched and never shown to it. I have read the
builder's report, which says where the table sits on the phone. I have not
opened the captures.

1. **It finds the mechanism twice** on weeks 2, 9, 11 and 12: once in the
   table's middle cell and again under "Why it works". This is the most likely
   finding, and I have decided to keep it, so I expect to route it to no one.
2. **It says the table is not near the top on the phone.** The builder
   measured the table's top at 954px on week 2 and 1036px on week 12, both
   below the first 844px. The task says "near the top of the page". A reader
   with no source should notice the first screen is a date, a chain of links
   and a paragraph.
3. **Week 10 is read as a page, not as a gap.** The order tells it the week
   has no trick. I expect no finding on the missing table.
4. **At least one finding is on a `seen` line,** most likely week 12's "A course
   website, where the marking rubric is published beside each assignment before
   anyone starts it.", which points at the site the reader is standing on.
5. **It reports desktop `scrollWidth` 1905 as given** and does not call it
   overflow. If it does, I will check whether the order made the 15px
   scrollbar clear.
6. **No finding says the phone table scrolls sideways.** Measured equal on
   every phone capture.

## What I would have done alone, for the gap

I would have checked the phone table by the numbers the builder gave me and
looked at one capture. I would not have counted where the table falls against
the first screen, because the builder's report said the prose was already
below the fold and I would have taken that as settled.

## What would make this prediction worthless

If the report contains none of 1, 2 and 4, the reader saw what I and the
builder already saw, and the evidence entry says so.
